{
  "name": "wewe-signal-server",
  "version": "1.0.0",
  "private": true,
  "description": "Stateless WebRTC signaling relay for Wewe: forwards SDP/ICE between exactly two peers (a monitor and a parent) sharing a pairing code. No media, no database, no accounts — self-hostable as a single process.",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc -p .",
    "start": "node dist/server.js",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.5",
    "@types/ws": "^8.5.13",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.2"
  }
}
